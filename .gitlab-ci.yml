image: microsoft/dotnet:latest

stages:
  - build
  - test

before_script:
  - "npm install"  
  
build:
  stage: build
  script:
    - "./node_modules/.bin/ng build --prod"

test:
  stage: test
  script:
    - "npm run test:ci"